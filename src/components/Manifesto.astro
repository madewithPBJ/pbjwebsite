---
---

<section id="manifesto" class="manifesto">
  <div class="manifesto__inner wrap">
    <p class="manifesto__line js-fade-up">KNOWN IN YOUR FIELD.</p>
    <p class="manifesto__line js-fade-up">SOUGHT BEYOND IT.</p>
  </div>
</section>

<style lang="scss">
  .manifesto {
    position: relative;
    z-index: 2;
    background: var(--cream-dark);
    padding: 72px 0;

    @media (max-width: 768px) {
      padding: 48px 0;
    }
  }

  .manifesto__inner {
    max-width: 1400px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .manifesto__line {
    font-family: var(--font-serif);
    font-size: clamp(36px, 6vw, 88px);
    font-weight: 600;
    text-transform: uppercase;
    color: var(--ink);
    line-height: 1.0;
    letter-spacing: -2px;
    white-space: nowrap;

    @media (max-width: 600px) {
      white-space: normal;
      font-size: clamp(36px, 9vw, 56px);
    }
  }
</style>
