---
---

<section id="manifesto" class="manifesto">
  <div class="manifesto__inner wrap">
    <blockquote class="manifesto__quote js-fade-up">
      KNOWN IN YOUR FIELD.<br />SOUGHT BEYOND IT.
    </blockquote>
  </div>
</section>

<style lang="scss">
  .manifesto {
    position: relative;
    z-index: 2;
    background: var(--cream-dark);
    padding: 160px 0;
    text-align: center;

    @media (max-width: 768px) {
      padding: 100px 0;
    }
  }

  .manifesto__inner {
    max-width: 1100px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .manifesto__quote {
    font-family: var(--font-serif);
    font-size: clamp(48px, 7vw, 96px);
    font-weight: 600;
    font-style: normal;
    text-transform: uppercase;
    color: var(--ink);
    line-height: 1.0;
    letter-spacing: -1px;

    @media (max-width: 600px) {
      br { display: none; }
    }
  }
</style>
