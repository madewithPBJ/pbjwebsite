---
---

<section id="hero" class="hero">
  <div class="hero__bg" aria-hidden="true"></div>
</section>

<style lang="scss">
  .hero {
    position: fixed;
    inset: 0;
    z-index: 1;
    background: var(--burgundy-deep);
    overflow: hidden;
  }

  .hero__bg {
    position: absolute;
    inset: 0;
    background: radial-gradient(
      ellipse at 50% 45%,
      rgba(61, 20, 84, 0.55) 0%,
      transparent 68%
    );
    pointer-events: none;
  }
</style>
